---
phase: 01-debug-screen-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - monitoni/ui/debug_screens/__init__.py
  - monitoni/ui/debug_screens/base.py
  - monitoni/ui/debug_screens/menu_screen.py
autonomous: true

must_haves:
  truths:
    - "BaseDebugSubScreen provides consistent header with back button"
    - "MenuScreen displays list of 7 component categories with icons"
    - "Menu items are tappable and invoke navigation callback"
  artifacts:
    - path: "monitoni/ui/debug_screens/__init__.py"
      provides: "Package exports"
      exports: ["BaseDebugSubScreen", "DebugMenuScreen"]
    - path: "monitoni/ui/debug_screens/base.py"
      provides: "Base class for sub-screens"
      contains: "class BaseDebugSubScreen"
      min_lines: 40
    - path: "monitoni/ui/debug_screens/menu_screen.py"
      provides: "Navigation hub menu"
      contains: "class DebugMenuScreen"
      min_lines: 60
  key_links:
    - from: "monitoni/ui/debug_screens/menu_screen.py"
      to: "navigate_callback"
      via: "on_release lambda"
      pattern: "on_release.*navigate"
    - from: "monitoni/ui/debug_screens/base.py"
      to: "navigate_back"
      via: "back button callback"
      pattern: "navigate_back"
---

<objective>
Create the debug sub-screen infrastructure: base class and navigation menu.

Purpose: Establish the foundation for the sub-screen architecture - a reusable base class that all sub-screens will inherit, and the main navigation menu that will be the entry point.

Output: Three new files in `monitoni/ui/debug_screens/` package providing BaseDebugSubScreen and DebugMenuScreen classes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-debug-screen-architecture/01-RESEARCH.md

# Existing code to reference for patterns
@monitoni/ui/debug_screen.py
@monitoni/ui/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create debug_screens package with base class</name>
  <files>
    monitoni/ui/debug_screens/__init__.py
    monitoni/ui/debug_screens/base.py
  </files>
  <action>
Create the `monitoni/ui/debug_screens/` directory and package.

**base.py - BaseDebugSubScreen class:**
- Inherit from `kivy.uix.screenmanager.Screen`
- Accept `navigate_back` callback in constructor (not hardcoded screen names)
- `title` as StringProperty with default "Sub-Screen"
- Build consistent UI in `__init__`:
  - Root `BoxLayout(orientation='vertical', padding="10dp")`
  - Header row with back button (MDRaisedButton "< Back", calls navigate_back) and title (MDLabel, font_style='H5')
  - `self.content` BoxLayout for subclass content (spacing="10dp", size_hint_y=None, bind minimum_height)
  - Wrap content in ScrollView for scrollable sub-screen content
- Provide `add_content(widget)` method for subclasses to add widgets

Use existing imports pattern from debug_screen.py. Use `"100dp"` and `"50dp"` for back button size (matches existing header style).

**__init__.py:**
- Export BaseDebugSubScreen from base
- (DebugMenuScreen will be added after Task 2)
  </action>
  <verify>
    python -c "from monitoni.ui.debug_screens.base import BaseDebugSubScreen; print('BaseDebugSubScreen imported successfully')"
  </verify>
  <done>
    BaseDebugSubScreen class exists with navigate_back callback, title property, header with back button, and scrollable content area.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DebugMenuScreen navigation hub</name>
  <files>
    monitoni/ui/debug_screens/menu_screen.py
    monitoni/ui/debug_screens/__init__.py
  </files>
  <action>
Create the navigation menu screen that lists all debug categories.

**menu_screen.py - DebugMenuScreen class:**
- Inherit from `kivy.uix.screenmanager.Screen`
- Accept `navigate_callback` and `back_to_customer_callback` in constructor
- Define MENU_ITEMS as class constant - list of tuples: (screen_name, icon, title, subtitle)
  - ('led', 'led-strip', 'LED Control', 'Brightness, colors, animations, zones')
  - ('relay', 'toggle-switch', 'Relay Control', 'Test relays, channel mapping')
  - ('sensor', 'door-sensor', 'Sensors', 'Door status, GPIO configuration')
  - ('audio', 'volume-high', 'Audio', 'Volume, test sounds')
  - ('motor', 'engine', 'Motor Settings', 'Timings, delays, test functions')
  - ('network', 'server-network', 'Network/Server', 'Endpoints, connection status')
  - ('stats', 'chart-bar', 'Statistics & Logs', 'View stats, export logs')

- Build UI:
  - Root BoxLayout(orientation='vertical', padding="10dp", spacing="10dp")
  - Header with Exit button (MDRaisedButton "< Exit", calls back_to_customer_callback) and title "Debug & Settings"
  - ScrollView containing MDList
  - For each menu item: TwoLineIconListItem with IconLeftWidget
  - on_release calls navigate_callback with screen_name

Use imports from kivymd.uix.list: MDList, TwoLineIconListItem, IconLeftWidget

**Update __init__.py:**
- Add export for DebugMenuScreen
  </action>
  <verify>
    python -c "from monitoni.ui.debug_screens import BaseDebugSubScreen, DebugMenuScreen; print('Both classes imported successfully')"
  </verify>
  <done>
    DebugMenuScreen exists with 7 menu items, each tappable and invoking navigate_callback with correct screen_name. Exit button invokes back_to_customer_callback.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Import verification:
   ```bash
   python -c "from monitoni.ui.debug_screens import BaseDebugSubScreen, DebugMenuScreen; print('OK')"
   ```

2. Class structure verification:
   ```bash
   python -c "
   from monitoni.ui.debug_screens import BaseDebugSubScreen, DebugMenuScreen
   # Check BaseDebugSubScreen has required attributes
   assert hasattr(BaseDebugSubScreen, 'title')
   # Check DebugMenuScreen has menu items
   assert hasattr(DebugMenuScreen, 'MENU_ITEMS')
   assert len(DebugMenuScreen.MENU_ITEMS) == 7
   print('Structure OK')
   "
   ```
</verification>

<success_criteria>
- [ ] `monitoni/ui/debug_screens/__init__.py` exports BaseDebugSubScreen and DebugMenuScreen
- [ ] `monitoni/ui/debug_screens/base.py` contains BaseDebugSubScreen with navigate_back callback
- [ ] `monitoni/ui/debug_screens/menu_screen.py` contains DebugMenuScreen with 7 menu items
- [ ] All imports succeed without error
- [ ] Menu items use TwoLineIconListItem with icons
</success_criteria>

<output>
After completion, create `.planning/phases/01-debug-screen-architecture/01-01-SUMMARY.md`
</output>
