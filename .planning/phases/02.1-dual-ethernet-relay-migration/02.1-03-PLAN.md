---
phase: 02.1-dual-ethernet-relay-migration
plan: 03
type: execute
wave: 3
depends_on: ["02.1-02"]
files_modified:
  - monitoni/ui/debug_screens/relay_screen.py
  - monitoni/ui/debug_screens/motor_screen.py
  - monitoni/ui/debug_screens/sensor_screen.py
autonomous: true
requirements:
  - ETH-UI-RELAY-SCREEN
  - ETH-UI-MOTOR-SCREEN
  - ETH-UI-SENSOR-SCREEN

must_haves:
  truths:
    - "Relay screen shows two sections: Core Module (8-CH) and Levels Module (30-CH)"
    - "Each relay section shows connection status header with IP, port, and green/red dot"
    - "Core module section has 8 relay test buttons, levels section has 30 relay test buttons"
    - "Channel labels show CH1, CH2 etc with function name as secondary info for mapped channels"
    - "Motor screen uses relay_core for motor/spindle operations"
    - "Sensor screen shows active method (GPIO or Modbus DI) as read-only indicator"
    - "Sensor screen keeps GPIO config section and adds Modbus DI info when active"
  artifacts:
    - path: "monitoni/ui/debug_screens/relay_screen.py"
      provides: "Dual-section relay settings screen"
      contains: "Core Module"
    - path: "monitoni/ui/debug_screens/motor_screen.py"
      provides: "Motor screen using relay_core"
      contains: "relay_core"
    - path: "monitoni/ui/debug_screens/sensor_screen.py"
      provides: "Sensor screen with method indicator"
      contains: "door_sensor"
  key_links:
    - from: "relay_screen.py"
      to: "hardware.relay_core"
      via: "self.hardware.relay_core"
      pattern: "self\\.hardware\\.relay_core"
    - from: "relay_screen.py"
      to: "hardware.relay_levels"
      via: "self.hardware.relay_levels"
      pattern: "self\\.hardware\\.relay_levels"
    - from: "motor_screen.py"
      to: "hardware.relay_core"
      via: "self.hardware.relay_core"
      pattern: "self\\.hardware\\.relay_core"
---

<objective>
Rebuild the relay settings screen for dual-module layout and update motor/sensor screens to reference the correct relay module and sensor method.

Purpose: These three UI screens are the operator-facing interface for the dual-module hardware. The relay screen gets a full rebuild with two sections. Motor and sensor screens get targeted updates to reference the correct module.

Output: Relay screen with Core Module and Levels Module sections. Motor screen using relay_core. Sensor screen showing active method with Modbus DI info.
</objective>

<execution_context>
@/home/admin/_DEV/MoniToniVending/.claude/get-shit-done/workflows/execute-plan.md
@/home/admin/_DEV/MoniToniVending/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-dual-ethernet-relay-migration/02.1-CONTEXT.md
@.planning/phases/02.1-dual-ethernet-relay-migration/02.1-02-SUMMARY.md

<interfaces>
<!-- HardwareManager interface after Plan 02 -->
From monitoni/hardware/manager.py:
```python
class HardwareManager:
    relay_core: Optional[RelayController]   # 8-CH module (motor, spindle, machine)
    relay_levels: Optional[RelayController]  # 30-CH module (door locks)
    relay: RelayController                   # DEPRECATED: alias for relay_core
    sensor: Optional[SensorController]       # GPIO or ModbusDigitalInputController

    async def spin_motor(duration_ms: int) -> bool: ...
    async def unlock_door(level: int) -> bool: ...
    async def lock_door(level: int) -> bool: ...
```

From monitoni/core/config.py:
```python
class EthernetRelayConfig(BaseModel):
    transport: str = "tcp"
    host: str = "192.168.1.100"
    port: int = 502
    slave_address: int = 1
    timeout: float = 1.0
    serial_port: str = "/dev/ttySC0"
    baudrate: int = 9600
    max_channels: int = 8

class DoorSensorConfig(BaseModel):
    method: str = "gpio"           # "gpio" or "modbus_di"
    gpio_pin: int = 5
    gpio_pull: str = "up"
    gpio_active: str = "low"
    di_index: int = 0
    poll_interval_ms: int = 150
```

<!-- Existing UI widget patterns -->
From monitoni/ui/debug_screens/widgets.py:
```python
SettingsCard(title: str)            # Card with coral header
HoldButton(text, on_hold, on_release_hold)  # Hold-to-activate
NumpadField(label, config_path, config_manager, ...)  # Tap-to-edit
LiveStatusCard(title, get_status_callback, update_interval)  # Async polling
show_confirm_dialog(title, text, on_confirm)
CORAL_ACCENT, NEAR_BLACK, INPUT_BUTTON, ERROR_RED  # Color constants
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild relay settings screen with dual-module layout</name>
  <files>monitoni/ui/debug_screens/relay_screen.py</files>
  <action>
Read existing `monitoni/ui/debug_screens/relay_screen.py` and rewrite it with dual-module layout per user decision: "One relay settings screen with two sections: Core Module (8-CH) and Levels Module (30-CH)".

**Layout structure (top to bottom):**

1. **Connection Status Header (Core Module):**
   - Row: MDLabel "Core Module" (bold) + connection dot (green/red) + IP:port text
   - Read from `config.hardware.relay_core` for IP/port display
   - Green dot if `hardware.relay_core.is_connected()`, red if not
   - Update via LiveStatusCard pattern (1s interval)

2. **Core Module Settings Card:**
   - Title: "Core Module (8-CH)"
   - Transport selector: Two buttons "TCP" / "Serial" — tapping saves to `hardware.relay_core.transport`
   - If TCP: show Host (TextInputDialog) + Port (NumpadField) config fields reading from `hardware.relay_core.host` and `hardware.relay_core.port`
   - If Serial: show Port string + Baudrate fields reading from `hardware.relay_core.serial_port` and `hardware.relay_core.baudrate`
   - Slave Address: NumpadField for `hardware.relay_core.slave_address`
   - Timeout: NumpadField for `hardware.relay_core.timeout`

3. **Core Module Relay Test Card:**
   - Title: "Core Relay Test"
   - Grid of 8 HoldButton relay buttons (4 cols x 2 rows), labeled "CH1" through "CH8"
   - Secondary labels for mapped channels: CH{motor_channel} shows "(Motor)", CH{spindle_channel} shows "(Spindle)"
   - on_hold -> `hardware.relay_core.set_relay(channel, True)`
   - on_release_hold -> `hardware.relay_core.set_relay(channel, False)`

4. **Connection Status Header (Levels Module):**
   - Same pattern as core but reads from `config.hardware.relay_levels`
   - Shows `hardware.relay_levels` connection state

5. **Levels Module Settings Card:**
   - Title: "Levels Module (30-CH)"
   - Same transport/host/port/serial/slave/timeout fields but for `hardware.relay_levels.*`

6. **Levels Module Relay Test Card:**
   - Title: "Levels Relay Test"
   - Grid of 30 HoldButton relay buttons (5 cols x 6 rows), labeled "CH1" through "CH30"
   - Channels that are mapped to door locks (from `vending.door_lock.relay_channels`) get visually highlighted — use CORAL_ACCENT as button background color for mapped channels, NEAR_BLACK for unmapped
   - on_hold -> `hardware.relay_levels.set_relay(channel, True)`
   - on_release_hold -> `hardware.relay_levels.set_relay(channel, False)`

7. **Door Lock Mapping Card:**
   - Keep existing door lock mapping UI (Level 1-10 -> relay channel NumpadField)
   - These channels now refer to channels on the LEVELS module (document this in card title: "Door Lock Mapping (Levels Module)")
   - Config path unchanged: `vending.door_lock.relay_channels`

8. **Live Status Card:**
   - Show relay_core status: Motor, Spindle relay states (from relay_core)
   - Show relay_levels status: First 3 door lock relays (from relay_levels)
   - Each line prefixed with module name: "Core: Motor (CH1)", "Levels: Door 1 (CH2)"

9. **Factory Reset button:**
   - Resets `hardware.relay_core`, `hardware.relay_levels`, and `vending.door_lock` sections

**Safety:** Cascade test removed from this screen (too dangerous with 30+8 channels across modules). Individual hold buttons are sufficient for testing.

Keep the existing patterns: BaseDebugSubScreen, SettingsCard, HoldButton, on_pre_leave safety cleanup.
  </action>
  <verify>
    <automated>cd /home/admin/_DEV/MoniToniVending && python -c "
from monitoni.ui.debug_screens.relay_screen import RelaySettingsScreen
# Verify class is importable and has expected structure
import inspect
source = inspect.getsource(RelaySettingsScreen)
assert 'relay_core' in source, 'Missing relay_core reference'
assert 'relay_levels' in source, 'Missing relay_levels reference'
assert 'Core Module' in source, 'Missing Core Module section'
assert 'Levels Module' in source or 'Levels' in source, 'Missing Levels Module section'
assert 'CH' in source, 'Missing channel labels'
print('Relay screen structure verified')
"</automated>
  </verify>
  <done>Relay screen shows two distinct sections for Core Module (8-CH) and Levels Module (30-CH) with independent connection status, settings, test buttons, and door lock mapping scoped to levels module.</done>
</task>

<task type="auto">
  <name>Task 2: Update motor and sensor screens for dual-module support</name>
  <files>monitoni/ui/debug_screens/motor_screen.py, monitoni/ui/debug_screens/sensor_screen.py</files>
  <action>
**Motor screen updates** (read existing `motor_screen.py` first):

1. Replace ALL occurrences of `self.hardware.relay` with `self.hardware.relay_core`. This is a find-and-replace across the entire file. Affected methods:
   - `_build_status_card` -> `get_motor_status()`: `self.hardware.relay_core`
   - `_start_motor_test`: check `self.hardware.relay_core`
   - `_run_motor_sequence`: `self.hardware.relay_core.set_relay(...)`
   - `_shutdown_motor_sequence`: `self.hardware.relay_core.set_relay(...)`
   - `_activate_spindle` / `_deactivate_spindle`: `self.hardware.relay_core.set_relay(...)`
   - `_activate_motor` / `_deactivate_motor`: `self.hardware.relay_core.set_relay(...)`
   - `on_pre_leave`: `self.hardware.relay_core.set_relay(...)`

2. Update the status card callback to show "Core Module" prefix:
   - "Core: Motor (CH{n})" instead of "Motor (R{n})"
   - "Core: Spindle (CH{n})" instead of "Spindle (R{n})"

3. Update the timing visualization text to mention "Core Module":
   - "1. Open spindle (Core CH{spindle_lock_relay})" etc.

4. Motor config fields remain unchanged — `vending.motor.relay_channel` and `vending.motor.spindle_lock_relay` still refer to channel numbers on the core module.

**Sensor screen updates** (read existing `sensor_screen.py` first):

1. Add a "Sensor Method" read-only indicator at the top of the screen:
   - Read `config.hardware.door_sensor.method` (or fall back to "gpio" if door_sensor config absent)
   - Display as a prominent label: "Active Method: GPIO" or "Active Method: Modbus Digital Input"
   - Use CORAL_ACCENT color for the method text
   - Add note: "(Change in config file)" — method switching is a config-level decision, not a UI toggle

2. Add "Modbus DI Info" card (shown only when method is modbus_di):
   - DI Index: read-only display of `door_sensor.di_index`
   - Poll Interval: read-only display of `door_sensor.poll_interval_ms` + "ms"
   - Source Module: "Core Module (8-CH)" with connection status dot
   - Host/Port: read from `config.hardware.relay_core.host`/`port`

3. Keep existing GPIO Configuration card as-is (always visible — it's the fallback config).
   - When method is modbus_di, add a subtle label to the GPIO card: "Fallback configuration (currently using Modbus DI)"

4. Keep existing Live Door Status card unchanged — it reads from `self.hardware.sensor` which is already dispatched by HardwareManager to the correct implementation.

5. Update Sensor Info card to show active method:
   - Add "Method: {method}" line
   - If modbus_di: show "DI Index: {n}", "Poll: {n}ms", "Source: Core Module {host}:{port}"
   - If gpio: show existing pin/pull/active info

6. Keep factory reset — it resets `hardware.gpio` section. When method is modbus_di, also reset `hardware.door_sensor` section.
  </action>
  <verify>
    <automated>cd /home/admin/_DEV/MoniToniVending && python -c "
import inspect
# Motor screen
from monitoni.ui.debug_screens.motor_screen import MotorSettingsScreen
motor_src = inspect.getsource(MotorSettingsScreen)
assert 'relay_core' in motor_src, 'Motor screen missing relay_core'
assert 'self.hardware.relay.' not in motor_src.replace('relay_core', 'XXX').replace('relay_levels', 'XXX'), 'Motor screen still has bare self.hardware.relay references (not relay_core/relay_levels)'
# Sensor screen
from monitoni.ui.debug_screens.sensor_screen import SensorSettingsScreen
sensor_src = inspect.getsource(SensorSettingsScreen)
assert 'door_sensor' in sensor_src or 'method' in sensor_src, 'Sensor screen missing method indicator'
print('Motor and sensor screens verified')
"</automated>
  </verify>
  <done>Motor screen routes all relay operations through relay_core. Sensor screen shows active method indicator (GPIO or Modbus DI) with relevant info card. Both screens use updated channel labeling convention.</done>
</task>

</tasks>

<verification>
- Relay screen imports and has dual-section structure
- Motor screen uses only relay_core (no bare self.hardware.relay references)
- Sensor screen shows active sensor method
- All three screens remain importable without runtime errors
</verification>

<success_criteria>
- Relay screen: two clearly separated module sections with independent settings and test buttons
- Motor screen: all relay operations go through relay_core
- Sensor screen: shows which method is active, displays Modbus DI info when applicable
- Channel labels use CH{n} format with function annotations for mapped channels
- Connection status visible per module in relay screen
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-dual-ethernet-relay-migration/02.1-03-SUMMARY.md`
</output>
