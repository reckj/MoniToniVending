---
phase: 02.1-dual-ethernet-relay-migration
plan: "04"
subsystem: hardware
tags: [relay, modbus, kivy, fastapi, telemetry]

# Dependency graph
requires:
  - phase: 02.1-02
    provides: HardwareManager with relay_core and relay_levels properties

provides:
  - All caller files updated to route relay operations to relay_core or relay_levels
  - Zero bare self.hardware.relay references remaining outside manager.py
  - Telemetry API accepts module parameter for dual-module relay control
  - Maintenance screen shows separate Core Relay and Levels Relay statuses

affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Relay caller pattern: motor/spindle ops use relay_core, door lock ops use relay_levels"
    - "Telemetry relay API: optional module=core|levels param, defaults to core"

key-files:
  created: []
  modified:
    - monitoni/ui/customer_screen.py
    - monitoni/ui/app.py
    - monitoni/telemetry/server.py
    - monitoni/ui/debug_screens/maintenance_screen.py
    - monitoni/main.py

key-decisions:
  - "Telemetry relay API defaults to module=core for backward compatibility with existing API clients"
  - "Cascade test endpoint accepts module param (core=8 channels, levels=30 channels)"
  - "app.py unlock_door guard changed to relay_levels since unlock_door uses relay_levels internally"

patterns-established:
  - "Door lock operations (unlock_door/lock_door) are guarded by relay_levels availability"
  - "Motor/spindle operations are guarded by relay_core availability"

# Metrics
duration: 2min
completed: 2026-03-24
---

# Phase 02.1 Plan 04: Remaining Caller Migration Summary

**All relay callers migrated to dual-module routing: motor/spindle ops use relay_core, door lock ops use relay_levels, zero bare .relay references remain outside manager.py**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-24T11:29:20Z
- **Completed:** 2026-03-24T11:31:08Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- customer_screen.py TurnButton motor/spindle relay calls routed to relay_core
- app.py door unlock guard updated from relay to relay_levels
- telemetry/server.py relay control API accepts module=core|levels param with module-aware routing and cascade test
- maintenance_screen.py status display shows "Core Relay" and "Levels Relay" separately
- main.py test_hardware function uses relay_core for relay test
- Final sweep confirmed zero bare .relay references remain outside manager.py

## Task Commits

Each task was committed atomically:

1. **Task 1: Update customer_screen.py and app.py relay references** - `5986198` (feat)
2. **Task 2: Update telemetry server, maintenance screen, and main.py** - `52263bb` (feat)

**Plan metadata:** (pending docs commit)

## Files Created/Modified

- `monitoni/ui/customer_screen.py` - TurnButton motor/spindle ops use relay_core
- `monitoni/ui/app.py` - _on_door_unlocked guard uses relay_levels
- `monitoni/telemetry/server.py` - /api/debug/relay and /api/debug/test-relay-cascade accept module param
- `monitoni/ui/debug_screens/maintenance_screen.py` - Machine Status shows Core Relay + Levels Relay rows
- `monitoni/main.py` - test_hardware uses relay_core

## Decisions Made

- Telemetry relay API defaults to `module="core"` for backward compatibility with API clients that don't pass a module param
- cascade test channel count derived from module: core=8, levels=30 (matches hardware spec)
- app.py door unlock guard uses `relay_levels` because `unlock_door()` is implemented in HardwareManager using relay_levels internally

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Dual-relay migration is fully complete across all callers
- Plans 02.1-01 through 02.1-04 complete the full Dual Ethernet Relay Migration phase
- Phase 02 (Settings Sub-screens) can resume with plans 02-02 onward

## Self-Check: PASSED

- All 5 modified files confirmed present on disk
- Both task commits (5986198, 52263bb) confirmed in git log
- Zero bare `.hardware.relay` references verified outside manager.py

---
*Phase: 02.1-dual-ethernet-relay-migration*
*Completed: 2026-03-24*
