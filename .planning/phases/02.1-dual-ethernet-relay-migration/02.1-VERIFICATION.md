---
phase: 02.1-dual-ethernet-relay-migration
verified: 2026-03-24T11:36:16Z
status: passed
score: 7/7 must-haves verified
re_verification: false
---

# Phase 02.1: Dual Ethernet Relay Migration — Verification Report

**Phase Goal:** Migrate from single RS485 relay + GPIO sensor to dual Ethernet PoE relay modules (Waveshare 30-CH for levels, 8-CH Module C for core) with digital input door sensing.
**Verified:** 2026-03-24T11:36:16Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Both relay modules controllable independently via Ethernet | VERIFIED | `EthernetRelayController` in `modbus_tcp_relay.py` implements `RelayController` with persistent TCP; `HardwareManager` creates `relay_core` and `relay_levels` instances independently |
| 2 | Door sensor reads from 8-CH digital input instead of GPIO | VERIFIED | `ModbusDigitalInputController` in `modbus_digital_input.py` reads FC02 DI over TCP; `HardwareManager._init_sensor()` dispatches on `door_sensor.method == "modbus_di"` using relay_core host/port |
| 3 | Motor/spindle operations route to core module | VERIFIED | `HardwareManager.spin_motor()` uses `self.relay_core`; `motor_screen.py` uses `self.hardware.relay_core` exclusively; `customer_screen.py` motor/spindle ops use `relay_core` |
| 4 | Door lock operations route to levels module | VERIFIED | `HardwareManager.unlock_door()` and `lock_door()` use `self.relay_levels`; `app.py` guard uses `relay_levels`; relay_screen.py levels test grid calls `hardware.relay_levels.set_relay()` |
| 5 | RS485 serial still works as alternative transport | VERIFIED | `HardwareManager._init_relay_core/levels()` has `transport == "serial"` branch creating `ModbusRelayController`; `modbus_relay.py` still imports CRC from `modbus_utils` and is otherwise unchanged |
| 6 | All existing relay/motor/sensor settings screens updated | VERIFIED | `relay_screen.py` rebuilt with Core Module (8-CH) and Levels Module (30-CH) dual sections; `motor_screen.py` uses only `relay_core`; `sensor_screen.py` shows active method indicator with conditional Modbus DI info card |
| 7 | Mock controllers still work for development without hardware | VERIFIED | `MockRelayController` (both core and levels), `MockDigitalInputController` all confirmed functional in mock mode; `HardwareManager(use_mock=True)` initializes both modules successfully |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Status | Evidence |
|----------|--------|----------|
| `monitoni/hardware/modbus_utils.py` | VERIFIED | Exports `modbus_crc`, `build_write_coil_frame`, `build_write_all_coils_frame`, `build_read_coils_frame`, `build_read_discrete_inputs_frame` — all tested and passing |
| `monitoni/hardware/modbus_tcp_relay.py` | VERIFIED | `EthernetRelayController` with persistent TCP, asyncio.Lock, `start/stop_reconnect_loop`, `connection_info` property — substantive and importable |
| `monitoni/hardware/modbus_digital_input.py` | VERIFIED | `ModbusDigitalInputController` FC02 over TCP with poll loop, `MockDigitalInputController` with `simulate_door_open/close` — both classes present and functional |
| `monitoni/hardware/base.py` | VERIFIED | `DigitalInputController` ABC extends `SensorController`; abstract `read_digital_input()`; concrete `get_door_state()` and `set_door_callback()` |
| `monitoni/core/config.py` | VERIFIED | `EthernetRelayConfig` and `DoorSensorConfig` pydantic models present; `HardwareConfig` extended with `Optional[EthernetRelayConfig]` relay_core/relay_levels and `Optional[DoorSensorConfig]` door_sensor |
| `config/default.yaml` | VERIFIED | `relay_core` (host 192.168.1.100, max_channels 8), `relay_levels` (host 192.168.1.101, max_channels 30), and `door_sensor` (method: gpio) sections present |
| `monitoni/hardware/manager.py` | VERIFIED | `relay_core`, `relay_levels` attributes; `relay` backward-compat property; dual `_init_relay_core/levels()` methods; config-driven `_init_sensor()`; partial operation on failure |
| `monitoni/ui/debug_screens/relay_screen.py` | VERIFIED | Dual sections — "Core Module (8-CH)" and "Levels Module (30-CH)" with independent connection status headers, settings cards, and test buttons |
| `monitoni/ui/debug_screens/motor_screen.py` | VERIFIED | All relay ops use `self.hardware.relay_core` exclusively; zero bare `.hardware.relay` references |
| `monitoni/ui/debug_screens/sensor_screen.py` | VERIFIED | Active method indicator card; conditional Modbus DI info card; GPIO card with fallback note; `door_sensor` config read |
| `monitoni/ui/customer_screen.py` | VERIFIED | Motor/spindle ops use `relay_core`; zero bare `.hardware.relay` refs |
| `monitoni/ui/app.py` | VERIFIED | Door unlock guard uses `relay_levels` |
| `monitoni/telemetry/server.py` | VERIFIED | `/api/debug/relay` and `/api/debug/test-relay-cascade` accept `module` param; routes to `relay_core` (default) or `relay_levels` |
| `monitoni/ui/debug_screens/maintenance_screen.py` | VERIFIED | Shows `relay_core` and `relay_levels` statuses separately |
| `monitoni/main.py` | VERIFIED | Test hardware function uses `relay_core` |

---

### Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|-----|--------|---------|
| `modbus_tcp_relay.py` | `modbus_utils.py` | `from monitoni.hardware.modbus_utils import modbus_crc` | WIRED | Line 15-19 of modbus_tcp_relay.py |
| `modbus_digital_input.py` | `modbus_utils.py` | `from monitoni.hardware.modbus_utils import build_read_discrete_inputs_frame` | WIRED | Line 16 of modbus_digital_input.py |
| `modbus_relay.py` | `modbus_utils.py` | `from monitoni.hardware.modbus_utils import modbus_crc` | WIRED | Line 13 of modbus_relay.py |
| `manager.py` | `modbus_tcp_relay.py` | `from monitoni.hardware.modbus_tcp_relay import EthernetRelayController` | WIRED | Line 18 of manager.py; used in `_init_relay_core()` and `_init_relay_levels()` |
| `manager.py` | `modbus_digital_input.py` | `from monitoni.hardware.modbus_digital_input import ModbusDigitalInputController, MockDigitalInputController` | WIRED | Line 19 of manager.py; used in `_init_sensor()` |
| `HardwareManager.spin_motor` | `self.relay_core` | `self.relay_core.set_relay` | WIRED | manager.py lines 508, 515 |
| `HardwareManager.unlock_door` | `self.relay_levels` | `self.relay_levels.set_relay` | WIRED | manager.py line 536 |
| `relay_screen.py` | `hardware.relay_core` | `self.hardware.relay_core` | WIRED | relay_screen.py lines 172, 450-456 |
| `relay_screen.py` | `hardware.relay_levels` | `self.hardware.relay_levels` | WIRED | relay_screen.py lines 175, 462-468 |
| `motor_screen.py` | `hardware.relay_core` | `self.hardware.relay_core` | WIRED | motor_screen.py lines 241, 249, 258, 322, 330 et al. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `relay_screen.py` connection status | `hardware.relay_core.is_connected()` | `EthernetRelayController.status` | Yes — live attribute on controller | FLOWING |
| `relay_screen.py` relay test | `hardware.relay_core.set_relay(channel, state)` | TCP socket write to physical module | Yes — no static return | FLOWING |
| `motor_screen.py` status | `hardware.relay_core.get_relay(channel)` | `_relay_states` cache populated on `set_relay` success | Yes — cache reflects sent commands | FLOWING |
| `sensor_screen.py` active method | `config.hardware.door_sensor.method` | `DoorSensorConfig` loaded from YAML | Yes — live config read | FLOWING |
| `sensor_screen.py` Modbus DI info | `config.hardware.relay_core.host/port` | `EthernetRelayConfig` from YAML | Yes — live config read | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| modbus_utils frame builders produce correct bytes | `build_write_coil_frame(1,1,True)` → 8-byte FC05 frame | len=8, frame[0]=1, frame[1]=5 | PASS |
| modbus_utils FC02 frame correct | `build_read_discrete_inputs_frame(1,0,1)` → fc02=0x02 | fc02[1]==2 confirmed | PASS |
| `build_write_all_coils_frame` targets 0x00FF | frame[2]=0x00, frame[3]=0xFF | Confirmed | PASS |
| HardwareManager mock mode initializes both modules | `HardwareManager(use_mock=True).initialize()` | results['relay_core']=True, results['relay_levels']=True | PASS |
| `relay` property backward compat | `hm.relay is hm.relay_core` | True | PASS |
| Motor/lock convenience methods in mock | `spin_motor(10)`, `unlock_door(1)`, `lock_door(1)` | All return True | PASS |
| Config loads dual-module sections | `ConfigManager().load().hardware.relay_core` | `max_channels=8`, `transport="tcp"` | PASS |
| Zero bare `.hardware.relay` refs in codebase | Regex sweep across all monitoni/*.py excl. manager.py | 0 matches | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status |
|-------------|------------|-------------|--------|
| ETH-TCP-TRANSPORT | 02.1-01 | EthernetRelayController with Modbus RTU over TCP | SATISFIED |
| ETH-RS485-FALLBACK | 02.1-01 | RS485 serial path still available via transport="serial" | SATISFIED |
| ETH-DIGITAL-INPUT | 02.1-01, 02.1-02 | ModbusDigitalInputController FC02 + manager dispatch | SATISFIED |
| ETH-CONFIG-RESTRUCTURE | 02.1-01 | EthernetRelayConfig, DoorSensorConfig, HardwareConfig extended | SATISFIED |
| ETH-DUAL-MODULE-ROUTING | 02.1-02 | relay_core (motor) and relay_levels (locks) routing in HardwareManager | SATISFIED |
| ETH-MOCK-CONTROLLERS | 02.1-02 | MockRelayController for both, MockDigitalInputController for DI | SATISFIED |
| ETH-PARTIAL-OPERATION | 02.1-02 | Per-module try/except with mock fallback | SATISFIED |
| ETH-UI-RELAY-SCREEN | 02.1-03 | Relay screen dual-section layout Core/Levels | SATISFIED |
| ETH-UI-MOTOR-SCREEN | 02.1-03 | Motor screen routes to relay_core | SATISFIED |
| ETH-UI-SENSOR-SCREEN | 02.1-03 | Sensor screen shows active method indicator | SATISFIED |
| ETH-CALLER-MIGRATION | 02.1-04 | customer_screen, app.py, telemetry, maintenance, main.py updated | SATISFIED |
| ETH-BACKWARD-COMPAT | 02.1-04 | `relay` property aliases relay_core; legacy modbus/gpio sections retained | SATISFIED |

---

### Anti-Patterns Found

None. No TODO/FIXME/placeholder patterns detected in phase files. No empty handler stubs. No hardcoded empty returns in data-serving paths. The only `return []`/`return {}` patterns are in error fallback paths that correctly create mock controllers.

---

### Human Verification Required

#### 1. TCP Connection to Real Hardware

**Test:** Configure `config/local.yaml` with actual Waveshare module IPs and transport: tcp. Start the app. Check connection dot in relay screen.
**Expected:** Green dot appears within 2 seconds. Relay test buttons activate physical relays.
**Why human:** Cannot test TCP to real hardware without physical modules present.

#### 2. FC02 Digital Input Polling with Real Module

**Test:** Configure `door_sensor.method: modbus_di` in local.yaml. Trigger door open/close on 8-CH module DI port.
**Expected:** Sensor screen live status updates within 150ms. Door callback fires in app logic.
**Why human:** Cannot test Modbus DI polling without physical 8-CH Module C connected.

#### 3. RS485 Serial Transport Path

**Test:** Configure `relay_core.transport: serial` with real serial port. Test relay buttons.
**Expected:** ModbusRelayController connects over serial, relay test buttons work.
**Why human:** Requires physical RS485 adapter and relay module.

#### 4. Relay Screen 30-Channel Grid Layout

**Test:** Open relay settings screen on 400x1280 touchscreen. Scroll through both sections.
**Expected:** 30 HoldButtons in 5-column grid are readable and tappable; mapped channels highlighted in coral.
**Why human:** Visual layout verification on physical display.

#### 5. Reconnect Loop Behavior

**Test:** Connect to real TCP relay module; unplug its Ethernet cable. Wait 2 seconds; reconnect cable.
**Expected:** Relay screen shows red dot while unplugged; reconnects automatically with backoff; green dot returns.
**Why human:** Cannot test background reconnect loop without real hardware and network control.

---

## Summary

Phase 02.1 goal is fully achieved. All 7 success criteria are met:

- **Dual TCP control:** `EthernetRelayController` implements raw Modbus RTU over TCP with persistent connection, asyncio lock, no-retry safety, and background reconnect. Both `relay_core` and `relay_levels` are independently managed.
- **Digital input door sensing:** `ModbusDigitalInputController` reads FC02 with 150ms poll loop and fires door callback on state change. `HardwareManager._init_sensor()` dispatches between GPIO and Modbus DI on config.
- **Correct routing:** Motor/spindle → `relay_core`; door locks → `relay_levels`. Zero bare `hardware.relay` references remain outside `manager.py`. Sweep confirmed across all 15 monitoni Python files.
- **RS485 fallback:** `transport: "serial"` branch in both `_init_relay_core()` and `_init_relay_levels()` creates `ModbusRelayController`.
- **UI updated:** Relay screen has dual-section layout with independent connection status, settings, and 8+30 test button grids. Motor screen fully migrated. Sensor screen shows active method with conditional Modbus DI card.
- **Mocks work:** `MockRelayController` (both modules), `MockDigitalInputController`, `MockSensorController` all tested in mock mode. `HardwareManager(use_mock=True)` fully functional.
- **Backward compat:** `self.relay` property returns `relay_core`; `modbus:` and `gpio:` sections in default.yaml retained.

5 items flagged for human verification — all require physical hardware (TCP modules, serial adapter, physical display). None block software correctness.

---

_Verified: 2026-03-24T11:36:16Z_
_Verifier: Claude (gsd-verifier)_
