---
status: testing
phase: 02.1-dual-ethernet-relay-migration
source: [02.1-01-SUMMARY.md, 02.1-02-SUMMARY.md, 02.1-03-SUMMARY.md, 02.1-04-SUMMARY.md]
started: 2026-03-24T12:00:00Z
updated: 2026-03-24T12:00:00Z
---

## Current Test

number: 1
name: App launches in mock mode
expected: |
  Run the app without hardware connected. It should start without errors — both relay modules fall back to MockRelayController, sensor falls back to mock. No crash, no unhandled exceptions in console.
awaiting: user response

## Tests

### 1. App launches in mock mode
expected: App starts without errors. Both relay modules fall back to mock controllers. No unhandled exceptions in console.
result: [pending]

### 2. Relay screen — dual-module layout
expected: Navigate to Debug > Relay. Screen shows two distinct sections: "Core Module (8-CH)" at top with 8 relay test buttons in a grid, and "Levels Module (30-CH)" below with 30 relay test buttons. Each section has its own connection status header showing IP:port and a colored dot.
result: [pending]

### 3. Relay screen — channel labels and mapped highlights
expected: In Core section, motor and spindle channels show function annotations (e.g., CH1 "(Motor)"). In Levels section, channels mapped to door locks are highlighted in coral color, unmapped channels are dark/neutral.
result: [pending]

### 4. Relay screen — core module settings
expected: Core Module settings card shows transport selector (TCP/Serial), host, port, slave address, timeout fields. Values match config/default.yaml (192.168.1.100:502). Fields are editable via NumpadField/TextInput.
result: [pending]

### 5. Relay screen — levels module settings
expected: Levels Module settings card shows same transport/host/port/slave/timeout fields but for the levels module. Values match config (192.168.1.101:502). Independent from core settings.
result: [pending]

### 6. Relay screen — door lock mapping
expected: Door Lock Mapping card title says "(Levels Module)" to clarify these channels are on the 30-CH module. Level 1-10 mapping fields present and editable.
result: [pending]

### 7. Motor screen — Core Module labels
expected: Navigate to Debug > Motor. Status card shows "Core: Motor (CH{n})" and "Core: Spindle (CH{n})" labels. Timing visualization references "Core CH{n}". No references to old "R{n}" format.
result: [pending]

### 8. Motor screen — hold-to-test works
expected: Hold a motor test button. Motor test sequence starts (in mock mode, completes without error). Release works cleanly. on_pre_leave safety cleanup fires when leaving screen.
result: [pending]

### 9. Sensor screen — method indicator
expected: Navigate to Debug > Sensors. Top of screen shows "Active Method: GPIO" (or "Modbus Digital Input" if config changed) in coral accent color with note "(Change in config file)".
result: [pending]

### 10. Sensor screen — GPIO config always visible
expected: GPIO Configuration card is visible regardless of active method. If method is modbus_di, card shows "Fallback configuration (currently using Modbus DI)" note.
result: [pending]

### 11. Sensor screen — live door status
expected: Live Door Status card shows door state and updates. In mock mode, shows mock sensor state without errors.
result: [pending]

### 12. Maintenance screen — dual module status
expected: Navigate to maintenance/debug status view. Machine Status shows "Core Relay" and "Levels Relay" as separate status rows (not a single "Relay" line).
result: [pending]

### 13. Customer screen — purchase flow doesn't crash
expected: Trigger a simulated purchase flow (select level, proceed). Motor/spindle operations don't throw errors about missing relay. Door lock operations don't throw errors. Mock controllers handle the calls.
result: [pending]

## Summary

total: 13
passed: 0
issues: 0
pending: 13
skipped: 0

## Gaps

[none yet]
