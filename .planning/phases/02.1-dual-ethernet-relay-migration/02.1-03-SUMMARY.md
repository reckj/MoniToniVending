---
phase: 02.1-dual-ethernet-relay-migration
plan: "03"
subsystem: ui
tags: [kivy, kivymd, relay, modbus, sensor, debug-screens]

# Dependency graph
requires:
  - phase: 02.1-02
    provides: HardwareManager with relay_core/relay_levels split and door_sensor dispatch

provides:
  - Relay settings screen with Core Module (8-CH) and Levels Module (30-CH) dual sections
  - Motor screen routing all relay ops through relay_core with Core Module channel labels
  - Sensor screen with active method indicator and conditional Modbus DI info card

affects:
  - phase 02.2 (any future UI plans referencing relay or sensor screens)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dual-section settings screen: independent status header, settings, and test card per module"
    - "Conditional card rendering: Modbus DI info card only shown when method == modbus_di"
    - "Read-only method indicator with CORAL_ACCENT label and '(Change in config file)' note"

key-files:
  created: []
  modified:
    - monitoni/ui/debug_screens/relay_screen.py
    - monitoni/ui/debug_screens/motor_screen.py
    - monitoni/ui/debug_screens/sensor_screen.py

key-decisions:
  - "Relay screen rebuilt with dual-section layout: Core Module (8-CH) and Levels Module (30-CH) each with independent status header, settings card, and relay test buttons"
  - "Method switching in sensor screen is read-only (config-level decision, not a UI toggle) — operator sees active method with a '(Change in config file)' note"
  - "GPIO card always visible as fallback config even when modbus_di is active — shows 'Fallback configuration' note"
  - "Factory reset on sensor screen resets hardware.door_sensor section when modbus_di is active"

patterns-established:
  - "Connection dot as colored MDLabel ('o' character) refreshed by Clock.schedule_interval at 1s"
  - "Mapped channels in Levels test grid highlighted with CORAL_ACCENT, unmapped with NEAR_BLACK"
  - "Status labels use 'Core: Motor (CH{n})' and 'Core: Spindle (CH{n})' prefix convention"

# Metrics
duration: 15min
completed: "2026-03-24"
---

# Phase 02.1 Plan 03: UI Screens Dual-Module Update Summary

**Relay screen rebuilt with dual Core/Levels module sections, motor screen updated to relay_core, sensor screen gains active method indicator with conditional Modbus DI info card**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-24T11:29:06Z
- **Completed:** 2026-03-24T11:44:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Relay settings screen has two distinct sections: Core Module (8-CH) with motor/spindle channel annotations, and Levels Module (30-CH) with door-lock-mapped channels highlighted in coral
- Motor screen routes all relay operations through `self.hardware.relay_core` — no bare `self.hardware.relay` references remain
- Sensor screen shows active method (GPIO or Modbus DI) as a prominent read-only indicator at the top, with a conditional Modbus DI info card showing DI index, poll interval, source module, and Core Module host:port

## Task Commits

1. **Task 1: Rebuild relay settings screen with dual-module layout** - `483eb90` (feat) — committed in prior session
2. **Task 2a: Update motor screen** - `7714207` (feat)
3. **Task 2b: Update sensor screen** - `3f3455d` (feat)

## Files Created/Modified

- `monitoni/ui/debug_screens/relay_screen.py` - Dual-module layout with Core (8-CH) and Levels (30-CH) sections; independent connection status, settings, test buttons; door lock mapping scoped to Levels Module
- `monitoni/ui/debug_screens/motor_screen.py` - All relay ops via relay_core; updated channel labels to `Core: Motor (CH{n})` convention; max_value for channel fields corrected to 8
- `monitoni/ui/debug_screens/sensor_screen.py` - Active method indicator card; GPIO card with fallback note when modbus_di active; conditional Modbus DI info card; updated Sensor Info to branch on method; factory reset includes hardware.door_sensor when modbus_di active

## Decisions Made

- Method switching in sensor screen is read-only — the label says "(Change in config file)" because it's a deployment-level configuration, not something operators should toggle on a live machine
- GPIO card stays visible at all times even when modbus_di is active, because GPIO pin config is the fallback and needs to be set correctly regardless

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None — relay screen had already been committed in a prior session before this execution started. Motor and sensor screen changes were staged.

## Known Stubs

None — all data is wired from live config and hardware references.

## Next Phase Readiness

- All three screens now correctly reference the dual-module hardware structure
- Plan 04 (customer screen + app.py relay reference updates) was previously committed as `5986198` — the full phase 02.1 migration is complete
- Ready for phase 02.2 or any downstream phase that references these screens

---
*Phase: 02.1-dual-ethernet-relay-migration*
*Completed: 2026-03-24*
